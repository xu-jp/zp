# 智能招聘系统部署说明

## 目录
- [系统架构](#系统架构)
- [环境要求](#环境要求)
- [后端部署](#后端部署)
- [前端部署](#前端部署)
- [AI服务配置](#ai服务配置)
- [数据库配置](#数据库配置)
- [部署验证](#部署验证)
- [常见问题](#常见问题)
- [生产环境部署](#生产环境部署)

## 系统架构

```
智能招聘系统
├── 后端 (Spring Boot 3.5.13 + Java 21)
│   ├── RESTful API
│   ├── JWT认证
│   ├── MyBatis-Plus ORM
│   ├── langchain4j AI服务集成
│   └── SSE流式输出支持
├── 前端 (Vue 3.4 + Element Plus 2.6)
│   ├── 用户中心（求职者）
│   ├── 企业中心（招聘者）
│   └── 管理后台
├── AI服务 (通义千问 Qwen)
│   ├── 简历优化（流式输出）
│   ├── 职位推荐
│   └── 简历筛选
└── 数据库 (MySQL 8.0)
```

## 环境要求

### 后端
- Java 21 或更高版本
- Maven 3.6 或更高版本
- MySQL 8.0 或更高版本

### 前端
- Node.js 18 或更高版本
- npm 或 yarn

### AI服务（可选）
- 通义千问 API Key（阿里云DashScope）

## 后端部署

### 1. 克隆项目
```bash
cd d:\code\intelligent-recruitment\backend
```

### 2. 配置数据库

编辑 `src/main/resources/application.yml` 文件：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/intelligent_recruitment2?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 你的数据库密码
```

### 3. 配置AI服务（可选）

在 `application.yml` 中配置通义千问 API：

```yaml
qwen:
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  api-key: 你的API-KEY
  model: qwen-plus

langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: 你的API-KEY
      model-name: qwen-plus
      log-requests: true
      log-responses: true
      timeout: PT60S
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: 你的API-KEY
      model-name: qwen-plus
      log-requests: true
      log-responses: true
      timeout: PT60S
```

**注意**：如果暂时不配置AI服务，系统会自动降级到规则匹配模式，不影响基本功能使用。

### 4. 编译和运行

```bash
# 编译项目
mvn clean package -DskipTests

# 运行
java -jar target/backend-0.0.1-SNAPSHOT.jar
```

或者直接运行：

```bash
mvn spring-boot:run
```

### 5. 验证后端启动

访问：`http://localhost:8080`

## 前端部署

### 1. 安装依赖

```bash
cd d:\code\intelligent-recruitment\frontend
npm install
```

### 2. 配置代理

前端已配置代理，查看 `vite.config.js`：

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

### 3. 开发模式运行

```bash
npm run dev
```

访问：`http://localhost:3000`

### 4. 生产环境构建

```bash
npm run build
```

构建产物在 `dist` 目录下。

## AI服务配置

### 通义千问API配置

系统支持两种AI模式：

#### 1. AI智能模式（推荐）

在 `application.yml` 中配置通义千问 API密钥：

```yaml
qwen:
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  api-key: sk-xxxxxxxxxxxxxxxxxxxxxxxx
  model: qwen-plus

langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-xxxxxxxxxxxxxxxxxxxxxxxx
      model-name: qwen-plus
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-xxxxxxxxxxxxxxxxxxxxxxxx
      model-name: qwen-plus
```

**功能**：
- AI简历优化：5维度分析（格式规范、完整性、关键词匹配、经历描述、技能展示）
- AI职位推荐：基于简历和职位库的智能匹配
- AI简历筛选：企业端的智能筛选功能
- 流式输出：支持SSE实时返回优化建议

#### 2. 规则匹配模式（降级方案）

如果不配置API密钥，系统会自动使用规则匹配：

**AI简历优化**：
- 格式规范评分
- 完整性评分
- 关键词匹配评分
- 经历描述评分
- 技能展示评分

**AI职位推荐**：
- 学历匹配度
- 地域匹配度
- 技能匹配度
- 经验匹配度

### 获取API Key

1. 访问 [阿里云DashScope](https://dashscope.aliyuncs.com/)
2. 注册并登录
3. 开通通义千问服务
4. 创建API Key

## 数据库配置

### 数据库初始化

```sql
# 创建数据库
CREATE DATABASE intelligent_recruitment2 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 使用数据库
USE intelligent_recruitment2;
```

### 数据表说明

| 表名 | 说明 |
|------|------|
| sys_user | 用户表（包含求职者、招聘者、管理员） |
| sys_job | 职位表 |
| sys_company | 企业表 |
| sys_resume | 简历表 |
| sys_application | 投递记录表 |
| sys_interview | 面试表 |
| sys_favorite | 收藏表 |
| sys_setting | 系统设置表 |
| sys_operation_log | 操作日志表 |

### 用户类型说明

| userType | 角色 |
|----------|------|
| 1 | 求职者 |
| 2 | 招聘者 |
| 3 | 管理员 |

## 部署验证

### 后端验证

1. **检查服务启动**
```bash
curl http://localhost:8080/api/auth/login
```

2. **测试登录接口**
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

3. **测试AI接口**
```bash
# AI简历优化
curl -X POST http://localhost:8080/api/resume/optimize/{resumeId} \
  -H "Authorization: Bearer {token}"

# AI职位推荐
curl http://localhost:8080/api/recommend/jobs?limit=5 \
  -H "Authorization: Bearer {token}"
```

### 前端验证

1. **检查页面加载**
   - 首页：`http://localhost:3000`
   - 登录：`http://localhost:3000/login`
   - 注册：`http://localhost:3000/register`

2. **测试功能**
   - 求职者：简历管理、职位投递、AI优化
   - 招聘者：职位发布、简历筛选、AI分析
   - 管理员：用户管理、企业审核、数据统计

## 常见问题

### 1. AI服务调用失败

**问题**：AI功能返回空数据或错误

**解决方案**：
- 检查API密钥是否正确
- 检查网络连接是否正常
- 检查API额度是否充足
- 系统会自动降级到规则匹配模式

### 2. 数据库连接失败

**问题**：启动时报数据库连接错误

**解决方案**：
```yaml
# 检查数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/intelligent_recruitment2?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 密码
```

### 3. 前端跨域问题

**问题**：前端请求后端接口报跨域错误

**解决方案**：
后端已配置CORS，如需修改请编辑 `WebConfig.java`：
```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("*")
                .allowedMethods("*")
                .allowedHeaders("*");
    }
}
```

### 4. 端口被占用

**问题**：端口8080已被占用

**解决方案**：
```yaml
# application.yml
server:
  port: 8081
```

### 5. 权限验证失败

**问题**：登录后访问受保护接口返回401或403

**解决方案**：
- 检查JWT密钥配置是否一致
- 检查Token是否过期
- 检查用户角色权限配置

### 6. Java版本问题

**问题**：启动报Java版本错误

**解决方案**：
- 确保使用Java 21或更高版本
- 检查JAVA_HOME环境变量

## 生产环境部署

### 后端生产部署

```bash
# 设置生产环境配置
export SPRING_PROFILES_ACTIVE=prod

# 运行
java -jar -Xms512m -Xmx1024m backend-0.0.1-SNAPSHOT.jar &
```

### 前端生产部署

```bash
# 构建
npm run build

# 使用Nginx部署
```

Nginx配置示例：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # SSE支持
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
    }
}
```

### Docker部署

#### 后端Dockerfile

```dockerfile
FROM eclipse-temurin:21-jdk-alpine
WORKDIR /app
COPY target/backend-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

#### 前端Dockerfile

```dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

#### docker-compose.yml

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: your_password
      MYSQL_DATABASE: intelligent_recruitment2
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      - mysql
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/intelligent_recruitment2?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: your_password

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
```

## 技术支持

- 项目地址：`d:\code\intelligent-recruitment`
- 文档：`README.md`
- 问题反馈：提交Issue

## 更新日志

### v1.0.0 (2024)
- ✅ AI简历优化（5维度分析，流式输出）
- ✅ AI职位推荐（智能匹配）
- ✅ AI简历筛选（企业端）
- ✅ 完整的权限管理系统
- ✅ 响应式前端界面
- ✅ 操作日志记录
- ✅ 系统设置管理
