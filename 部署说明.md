# 智能招聘系统部署说明

## 目录
- [系统架构](#系统架构)
- [环境要求](#环境要求)
- [后端部署](#后端部署)
- [前端部署](#前端部署)
- [AI服务配置](#ai服务配置)
- [数据库配置](#数据库配置)
- [部署验证](#部署验证)
- [常见问题](#常见问题)

## 系统架构

```
智能招聘系统
├── 后端 (Spring Boot 3.5.13)
│   ├── RESTful API
│   ├── JWT认证
│   ├── MyBatis-Plus ORM
│   └── Qwen大模型集成
├── 前端 (Vue 3 + Element Plus)
│   ├── 用户中心（求职者）
│   ├── 企业中心（招聘者）
│   └── 管理后台
└── 数据库 (MySQL 8.0)
```

## 环境要求

### 后端
- Java 17 或更高版本
- Maven 3.6 或更高版本
- MySQL 8.0 或更高版本
- Node.js 18 或更高版本（用于构建）

### 前端
- Node.js 18 或更高版本
- npm 或 yarn

## 后端部署

### 1. 克隆项目
```bash
cd d:\code\intelligent-recruitment\backend
```

### 2. 配置数据库

编辑 `src/main/resources/application.yml` 文件：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/intelligent_recruitment2?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 你的数据库密码
```

### 3. 配置Qwen API

在 `application.yml` 中配置Qwen API密钥：

```yaml
qwen:
  api-key: 你的Qwen-API-Key
  model: qwen-plus
  base-url: https://dashscope.aliyuncs.com/api/v1
```

**注意**：如果暂时不配置Qwen API，系统会自动降级到规则匹配模式。

### 4. 编译和运行

```bash
# 编译项目
mvn clean package -DskipTests

# 运行
java -jar target/backend-0.0.1-SNAPSHOT.jar
```

或者直接运行：

```bash
mvn spring-boot:run
```

### 5. 验证后端启动

访问：`http://localhost:8080/api/auth/login`

## 前端部署

### 1. 安装依赖

```bash
cd d:\code\intelligent-recruitment\frontend
npm install
```

### 2. 配置代理

编辑 `vite.config.js`（如果不存在则创建）：

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        secure: false
      }
    }
  }
})
```

### 3. 开发模式运行

```bash
npm run dev
```

访问：`http://localhost:5173`

### 4. 生产环境构建

```bash
npm run build
```

构建产物在 `dist` 目录下。

## AI服务配置

### Qwen API配置

系统支持两种AI模式：

#### 1. AI智能模式（推荐）

在 `application.yml` 中配置Qwen API密钥：

```yaml
qwen:
  api-key: sk-xxxxxxxxxxxxxxxxxxxxxxxx
  model: qwen-plus
  base-url: https://dashscope.aliyuncs.com/api/v1
```

**功能**：
- AI简历优化：5维度分析（格式规范、完整性、关键词匹配、经历描述、技能展示）
- AI职位推荐：基于简历和职位库的智能匹配
- AI简历筛选：企业端的智能筛选功能

#### 2. 规则匹配模式（降级方案）

如果不配置Qwen API密钥，系统会自动使用规则匹配：

**AI简历优化**：
- 格式规范评分
- 完整性评分
- 关键词匹配评分
- 经历描述评分
- 技能展示评分

**AI职位推荐**：
- 学历匹配度
- 地域匹配度
- 技能匹配度
- 经验匹配度
- 随机权重调整

## 数据库配置

### 数据库初始化

执行SQL脚本初始化数据库：

```bash
# 创建数据库
CREATE DATABASE intelligent_recruitment2 CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入数据表结构
mysql -u root -p intelligent_recruitment2 < backend/src/main/resources/schema.sql
```

### 数据表说明

| 表名 | 说明 |
|------|------|
| user | 用户表 |
| resume | 简历表 |
| job | 职位表 |
| company | 企业表 |
| application | 投递记录表 |
| favorite | 收藏表 |
| interview | 面试表 |
| system_setting | 系统设置表 |

## 部署验证

### 后端验证

1. **检查服务启动**
```bash
curl http://localhost:8080/api/auth/login
```

2. **测试AI接口**
```bash
# AI简历优化
curl -X POST http://localhost:8080/api/resume/optimize/{resumeId} \
  -H "Authorization: Bearer {token}"

# AI职位推荐
curl http://localhost:8080/api/recommend/jobs?limit=5 \
  -H "Authorization: Bearer {token}"
```

### 前端验证

1. **检查页面加载**
   - 首页：`http://localhost:5173`
   - 登录：`http://localhost:5173/login`
   - 注册：`http://localhost:5173/register`

2. **测试功能**
   - 求职者：简历管理、职位投递、AI优化
   - 招聘者：职位发布、简历筛选、AI分析
   - 管理员：用户管理、企业审核、数据统计

## 常见问题

### 1. Qwen API调用失败

**问题**：AI功能返回空数据或错误

**解决方案**：
- 检查API密钥是否正确
- 检查网络连接是否正常
- 检查API额度是否充足
- 系统会自动降级到规则匹配模式

### 2. 数据库连接失败

**问题**：启动时报数据库连接错误

**解决方案**：
```yaml
# 检查数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/数据库名?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 密码
```

### 3. 前端跨域问题

**问题**：前端请求后端接口报跨域错误

**解决方案**：
```javascript
// vite.config.js
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        secure: false
      }
    }
  }
})
```

### 4. 端口被占用

**问题**：端口8080已被占用

**解决方案**：
```yaml
# application.yml
server:
  port: 8081
```

### 5. 权限验证失败

**问题**：登录后访问受保护接口返回401或403

**解决方案**：
- 检查JWT密钥配置是否一致
- 检查Token是否过期
- 检查用户角色权限配置

## 高级配置

### 生产环境部署

#### 后端生产部署

```bash
# 设置生产环境配置
export SPRING_PROFILES_ACTIVE=prod

# 运行
java -jar -Xms512m -Xmx1024m backend-0.0.1-SNAPSHOT.jar &
```

#### 前端生产部署

```bash
# 构建
npm run build

# 使用Nginx部署
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Docker部署

#### 后端Dockerfile

```dockerfile
FROM openjdk:17-jdk-slim
WORKDIR /app
COPY target/backend-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
```

#### 前端Dockerfile

```dockerfile
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

## 技术支持

- 项目地址：`d:\code\intelligent-recruitment`
- 文档：`README.md`
- 问题反馈：提交Issue

## 更新日志

### v1.0.0 (2024)
- ✅ AI简历优化（5维度分析）
- ✅ AI职位推荐（智能匹配）
- ✅ AI简历筛选（企业端）
- ✅ 完整的权限管理系统
- ✅ 响应式前端界面
