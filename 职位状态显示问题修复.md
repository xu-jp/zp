# 职位状态显示问题修复总结

## 问题描述

招聘者在职位管理页面发现，发布的职位状态明明是上架的，但页面显示为"已下架"。

## 问题分析

### 根本原因

职位状态显示不一致的原因是：

1. **职位状态字段**：
   - `status`：职位上架状态（0=已下架，1=招聘中）
   - `auditStatus`：职位审核状态（0=待审核，1=审核通过，2=审核拒绝）

2. **完整状态流程**：
   - 职位发布时：`status = 1`（上架），`auditStatus = 0`（待审核）
   - 审核通过后：`auditStatus = 1`（审核通过）
   - 下架时：`status = 0`（已下架）

3. **问题所在**：
   - 后端在返回职位列表时，没有设置 `auditStatus` 字段
   - 前端只检查 `status` 字段，没有考虑 `auditStatus`
   - 导致职位虽然 `status = 1`（上架），但因为 `auditStatus = 0`（待审核），实际上不能显示为"招聘中"

## 修复方案

### 后端修复

#### 1. 修复 `CompanyJobServiceImpl.getCompanyJobs()` 方法

**文件**：`backend/src/main/java/com/xu/service/impl/CompanyJobServiceImpl.java`

**修改内容**：在返回 `JobVO` 时添加 `auditStatus` 字段

```java
JobVO vo = new JobVO();
vo.setId(job.getId());
vo.setTitle(job.getTitle());
vo.setCategory(job.getCategory());
vo.setLocation(job.getLocation());
vo.setSalaryMin(job.getSalaryMin());
vo.setSalaryMax(job.getSalaryMax());
vo.setExperience(job.getExperience());
vo.setEducation(job.getEducation());
vo.setStatus(job.getStatus());
vo.setAuditStatus(job.getAuditStatus());  // 新增
vo.setViewCount(job.getViewCount());
vo.setApplicationCount(job.getApplicationCount());
vo.setCreateTime(job.getCreateTime());
```

#### 2. 修复 `CompanyJobServiceImpl.getJobDetail()` 方法

**修改内容**：在返回 `JobVO` 时添加 `auditStatus` 字段

```java
JobVO vo = new JobVO();
vo.setId(job.getId());
vo.setTitle(job.getTitle());
vo.setCategory(job.getCategory());
vo.setLocation(job.getLocation());
vo.setSalaryMin(job.getSalaryMin());
vo.setSalaryMax(job.getSalaryMax());
vo.setExperience(job.getExperience());
vo.setEducation(job.getEducation());
vo.setStatus(job.getStatus());
vo.setAuditStatus(job.getAuditStatus());  // 新增
vo.setViewCount(job.getViewCount());
vo.setApplicationCount(job.getApplicationCount());
vo.setCreateTime(job.getCreateTime());
```

### 前端修复

#### 1. 修复 `jobs.vue` 职位列表页面

**文件**：`frontend/src/views/company/jobs.vue`

**修改内容**：更新状态列显示逻辑

**修改前**：
```vue
<el-table-column label="状态" width="100">
  <template #default="{ row }">
    <el-tag :type="row.status === 1 ? 'success' : 'info'">
      {{ row.status === 1 ? '招聘中' : '已下架' }}
    </el-tag>
  </template>
</el-table-column>
```

**修改后**：
```vue
<el-table-column label="状态" width="120">
  <template #default="{ row }">
    <el-tag v-if="row.auditStatus !== 1" type="warning">
      待审核
    </el-tag>
    <el-tag v-else-if="row.status === 1" type="success">
      招聘中
    </el-tag>
    <el-tag v-else type="info">
      已下架
    </el-tag>
  </template>
</el-table-column>
```

**修改内容**：更新操作按钮显示逻辑

**修改前**：
```vue
<el-button 
  :type="row.status === 1 ? 'warning' : 'success'" 
  link 
  @click="handleToggleStatus(row)"
>
  {{ row.status === 1 ? '下架' : '上架' }}
</el-button>
```

**修改后**：
```vue
<el-button 
  v-if="row.auditStatus === 1"
  :type="row.status === 1 ? 'warning' : 'success'" 
  link 
  @click="handleToggleStatus(row)"
>
  {{ row.status === 1 ? '下架' : '上架' }}
</el-button>
<el-button 
  v-else
  type="primary" 
  link 
  disabled
>
  待审核
</el-button>
```

#### 2. 修复 `detail.vue` 职位详情页面

**文件**：`frontend/src/views/jobs/detail.vue`

**修改内容**：添加职位状态显示

```vue
<div class="job-status">
  <el-tag v-if="job.auditStatus !== 1" type="warning">
    待审核
  </el-tag>
  <el-tag v-else-if="job.status === 1" type="success">
    招聘中
  </el-tag>
  <el-tag v-else type="info">
    已下架
  </el-tag>
</div>
```

## 状态显示逻辑

### 完整的状态判断流程

```
1. 检查 auditStatus
   ├─ auditStatus !== 1 → 显示"待审核"（黄色标签）
   └─ auditStatus === 1 → 继续检查 status
       ├─ status === 1 → 显示"招聘中"（绿色标签）
       └─ status === 0 → 显示"已下架"（灰色标签）
```

### 状态说明

| auditStatus | status | 显示状态 | 说明 |
|------------|--------|---------|------|
| 0 | 1 | 待审核 | 职位已发布但未通过审核 |
| 0 | 0 | 待审核 | 职位已下架但未通过审核 |
| 1 | 1 | 招聘中 | 职位已通过审核且上架 |
| 1 | 0 | 已下架 | 职位已通过审核但已下架 |
| 2 | 1 | 待审核 | 职位被审核拒绝 |
| 2 | 0 | 待审核 | 职位被审核拒绝且已下架 |

## 测试验证

### 测试场景

1. **发布新职位**
   - 预期：状态显示"待审核"
   - 操作：点击"上架"按钮应显示"待审核"（禁用状态）

2. **审核通过后**
   - 预期：状态显示"招聘中"（绿色标签）
   - 操作：可以点击"下架"按钮

3. **下架职位**
   - 预期：状态显示"已下架"（灰色标签）
   - 操作：可以点击"上架"按钮

4. **审核拒绝**
   - 预期：状态显示"待审核"
   - 操作：需要重新编辑并提交审核

## 注意事项

1. **职位发布流程**：
   - 职位发布时 `status = 1`（上架），`auditStatus = 0`（待审核）
   - 职位需要管理员审核通过后才能真正显示为"招聘中"

2. **上架/下架操作**：
   - 只有审核通过的职位（`auditStatus = 1`）才能进行上架/下架操作
   - 未审核的职位显示"待审核"按钮（禁用状态）

3. **状态一致性**：
   - 前端和后端的状态显示逻辑保持一致
   - 所有职位列表和详情页面都使用相同的状态判断逻辑

## 修复完成

✅ 后端：添加 `auditStatus` 字段返回
✅ 前端：职位列表页面状态显示修复
✅ 前端：职位详情页面状态显示修复
✅ 前端：操作按钮逻辑修复
